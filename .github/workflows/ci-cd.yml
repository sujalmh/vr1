name: CI/CD Pipeline

on:
  push:
    branches:
      - dev       # development
      - test      # business testing
      - main      # production

jobs:
  dev:
    if: github.ref == 'refs/heads/dev'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to DEV via SSH (No Docker)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEV_HOST }}
          username: ${{ secrets.DEV_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /mnt/data/sujalmh/vr
            git pull origin dev
            fuser -k 7210/tcp || true
            fuser -k 7220/tcp || true
            
            cd loader
            source venv/bin/activate
            pip install -r requirements.txt
            nohup uvicorn loader.main:app --host 0.0.0.0 --port 7210 > loader.log 2>&1 &
            deactivate

            cd ../retrieval
            source venv/bin/activate
            pip install -r requirements.txt
            nohup uvicorn retrieval.main:app --host 0.0.0.0 --port 7220 > retrieval.log 2>&1 &
            deactivate

  test:
    if: github.ref == 'refs/heads/test'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to TEST
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.TEST_HOST }}
          username: ${{ secrets.TEST_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /mnt/data/sujalmh/vr
            git pull origin test
            bash deploy.sh loader test 7211
            bash deploy.sh retrieval test 7221

  prod:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy to PROD (Docker on Port 7000)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /mnt/data/sujalmh/vr
            git pull origin main
            bash deploy.sh loader prod 7213
            bash deploy.sh retrieval prod 7223
